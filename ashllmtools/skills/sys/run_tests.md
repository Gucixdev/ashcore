---
name: run_tests
category: code
---

Run the project test suite via `pixi run test`.

**Input:** directory containing `pixi.toml` (empty = current dir)
**Output:** combined stdout/stderr from the test runner
