---
name: bughunt
category: code
---

Locate the root cause of a bug given symptoms or a failing test.

**Input:** error message, stack trace, or failing test output
**Output:** suspected root cause + file/line if identifiable

Stub — combine `search` + `read_file` + `reflect` for a real pipeline.
